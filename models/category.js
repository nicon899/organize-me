class Category {
    constructor(id, name, value, parentId) {
        this.id = id;
        this.name = name;
        this.value = value;
        this.parentId = parentId;
    };
}

export default Category;
